export class Example {
    add(a: number, b: number) {
        return a + b;
    }

    fizzBuzz(number: number): any {
        if (number === 1) {
            return "1";
        }
        return "Fizz";
    }
}